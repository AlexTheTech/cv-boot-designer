import React from 'react'
import ReactDOM from 'react-dom/client'
import CVBootDesigner from './CVBootDesigner'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CVBootDesigner />
  </React.StrictMode>,
)